### 2026-07-24 (полная документация проекта)

- Создана комплексная документация проекта, включающая архитектурное описание, руководства по контенту, разработке и развертыванию.
- Обновлен `README.md`: теперь он содержит актуальный технологический стек (Astro, React, Directus, Platega), структуру проекта и инфраструктурную схему.
- Добавлен `docs/architecture.md`: описаны основные архитектурные слои, работа SSR, Middleware для проксирования S3 и разделение серверной/клиентской логики.
- Добавлен `docs/content.md`: руководство по работе с Content Collections (блог), формату фронтматтера и статическим мокам.
- Добавлен `docs/development.md`: инструкции по настройке локального окружения, переменным .env и основным командам разработки.
- Добавлен `docs/deployment.md`: описание процесса Docker-сборки, настройки Dokploy (build args vs runtime env) и интеграции с MinIO.
- Все технические подробности по оплате сохранены и структурированы в `docs/platega-directus.md`.

### 2026-07-23 (упрощение логики упражнений)

- Состояние упражнения описано одним DTO `ExerciseStateData`.
- Переходы вынесены в небольшой reducer-команду: ввод, проверка, повтор,
  подсказка и сдача. Компонент оставлен только для UI и навигации.

### 2026-07-23 (повторная проверка ответа в упражнениях)

- После неверного ответа кнопка «Ещё раз проверить» возвращает форму в режим
  редактирования с сохранённым текстом и фокусом в поле.
- Повторный клик больше не проверяет и не засчитывает тот же неверный ответ
  второй раз.

### 2026-07-23 (исправление checkout месячного тарифа)

- Для месячного тарифа закреплён поддерживаемый merchant account метод оплаты
  `paymentMethod: 2`. Метод `6` (`Subscription`) отклонялся Platega с
  `Wrong input parameters`.

### 2026-07-23 (универсальный навык Platega)

- Создан переносимый навык `.junie/skills/platega/SKILL.md` для безопасной интеграции Platega.io: разовые платежи, платёжные ссылки с заданным методом и без него, проверка статуса, H2H QR, три callback-потока, рекуррентные СБП-подписки, возвраты, балансы, выгрузки и Payout API.
- Зафиксированы критические инварианты: merchant secrets доступны только серверу, сумма и тариф определяются backend-ом, поле `id` при создании не передаётся, success/failed redirect не считается подтверждением оплаты, callback-и проверяются и обрабатываются идемпотентно со сверкой суммы и валюты.
- Добавлены references по актуальным API-контрактам, безопасности callback-ов и точной HMAC-подписи Payout; отдельно описаны расхождения документации Platega в casing, enum-схемах и response fields.
- Добавлены переносимые TypeScript-шаблоны server client, DTO, callback auth/parser и отдельного Payout client с явным `Idempotency-Key`.
- Навык зарегистрирован в `.junie/skills/SKILL.md` и `AGENTS.md`; его использование обязательно для всех задач Platega.
- Фактическое подключение оплаты к `/checkout` в рамках этого этапа не выполнялось; текущая страница остаётся статической, а размещение secrets требует отдельного backend/BFF или осознанного перехода на server runtime.

### 2026-07-23 (авторизация через Directus)

- Создан переносимый навык `.junie/skills/directus-auth/SKILL.md` для повторной реализации авторизации Directus в Astro + React проектах. В пакет входят архитектурные и security-инструкции, настройка Directus/CORS, шаблоны SDK storage, React-хука и формы, guest/protected guards, logout и сценарный чек-лист.
- Навык зарегистрирован в `.junie/skills/SKILL.md` и `AGENTS.md`; для задач login/signup/session/refresh/guards/logout его использование обязательно.
- Подключены регистрация (`POST /users/register`) и вход (`POST /auth/login`) через Directus по адресу `http://english-directus-e543fd-77-110-104-179.sslip.io`.
- После регистрации выполняется автоматический вход; после успешной авторизации пользователь перенаправляется в `/cabinet`.
- Access token сохраняется в `localStorage` под ключом `directus_access_token`; refresh token и время истечения сохраняются под ключами `directus_refresh_token` и `directus_token_expires`.
- Установлен официальный `@directus/sdk`; клиент с SDK storage adapter вынесен в `src/lib/directus.ts`, состояние форм — в React-хук `src/hooks/useDirectusAuth.ts`.
- Создан общий компонент `AuthForm.tsx`, подключённый к основной и модальным формам входа/регистрации на главной странице.
- Главная страница проверяет активную Directus-сессию до показа форм и перенаправляет уже авторизованного пользователя в `/cabinet` через `location.replace`, поэтому возврат назад не открывает форму авторизации.
- Добавлен `ProtectedPageGuard.astro` и подключён к `/cabinet`: до проверки сессии содержимое скрыто, а гость, пользователь с невалидным токеном или при недоступности Directus перенаправляется на `/`.
- Кнопка «Выход» в общем Header завершает Directus-сессию, очищает локальные access/refresh-токены даже при ошибке API и возвращает пользователя на `/`.
- Добавлена публичная переменная `PUBLIC_DIRECTUS_URL` в `.env.example` с рабочим адресом Directus по умолчанию.
- Docker-сборка принимает `PUBLIC_DIRECTUS_URL` как безопасный публичный build
  argument; серверные Directus и Platega credentials читаются из runtime
  environment контейнера, поэтому секреты Dokploy не вшиваются в image.
- `npm run build` проходит успешно.

### 2026-07-22 (исправление формы упражнений)

- Устранил дублирование введённого ответа при проверке: редактирование и отображение результата теперь разделены, React больше не управляет одновременно состоянием и изменённым вручную `contentEditable` DOM.
- Исправил повторную попытку после неверного ответа и после действия «Сдаться»: введённый текст сохраняется, форма остаётся на месте, фокус возвращается в поле ввода.
- Изолировал React-форму упражнений от устаревших jQuery-обработчиков, чтобы один клик не запускал параллельную проверку через несуществующие `/cabinet/*` API-маршруты.

### 2026-07-21 (инфраструктура)

- Упростил логику проксирования файлов из MinIO: теперь запросы `/uploads/*` проксируются «как есть» к базовому URL хранилища (`.../english`), без использования `rewrite` и ручной замены путей.
- Обновил `.env`, `.env.example`, `astro.config.mjs` и `src/middleware.js` для соответствия новой схеме.
- Актуализировал переменную `MINIO` (убрал префикс `/uploads/` из базового адреса).
- Перевел Middleware с редиректа на проксирование (`fetch`). Это позволяет использовать внутренние адреса Docker-контейнеров (например, `http://minio:9000`) для ускорения трафика внутри сети Dokploy и скрытия S3-хранилища от пользователей.
- Обновил `src/middleware.js` для проброса заголовков (включая Range для видео) при проксировании.
- Вынес адрес хранилища MinIO в переменную окружения `MINIO`.
- Провел радикальную очистку репозитория: переинициализировал Git, удалив историю объемом 4 ГБ (тяжелые медиафайлы и сборки).
- Размер `.git` уменьшен с 4 ГБ до 7.5 МБ; настроил `.gitignore` и `.dockerignore` для исключения `/public/uploads/`.
- Физически удалил папку `dist/` и очистил медиафайлы в `public/uploads/`, так как теперь они проксируются из внешнего хранилища.

### 2026-07-21 (упражнения)

- Реализовал модальное окно завершения тренировки `modalRemindRepeat` с отображением статистики (всего пройдено, верно, неверно).
- Заменил стандартный экран завершения на всплывающее окно согласно предоставленной структуре и стилям проекта.
- Добавил логику кнопки «Завершить» на последнем вопросе темы, которая вызывает окно результатов.
- Реализовал функциональность кнопок «Повторить» (сброс на первую страницу) и «Закрыть» (скрытие модалки) внутри окна завершения.
- Исправил баг с доступностью поля ввода в упражнениях: теперь редактирование разрешено только для текущего активного вопроса.
- Реализовал режим просмотра для пройденных вопросов: при переходе назад отображается правильный ответ (подсвечен зеленым), а поле ввода блокируется.
- Заблокировал возможность ввода ответа для будущих вопросов (при ручной смене URL): поле ввода и кнопки действий (Проверить, Сдаться, Подсказака) отключаются, пока пользователь не дойдет до этого вопроса по порядку.
- Улучшил синхронизацию состояния: при навигации через стрелки или пагинацию состояние корректно сбрасывается или восстанавливается в зависимости от `progress.correct`.
- Разблокировал навигационные стрелки в пагинации: теперь пользователь может свободно перемещаться между вопросами для ознакомления, даже если они еще не решены (поле ввода при этом остается заблокированным).
- Исправил блокировку поля ввода после проверки: теперь при неверном ответе поле остается доступным для редактирования (при клике на поле подсказки скрываются, и можно исправить текст).
- Перевёл навигацию в личном кабинете на систему редиректов/перезагрузок (window.location.href): теперь при выборе темы, переходе по страницам или автоматическом переходе после правильного ответа страница полностью перезагружается, что устраняет проблемы с «пустым экраном» и рассинхроном URL.
- Добавил задержку 800мс перед редиректом после успешного ответа, чтобы пользователь успел увидеть результат.
- Полностью удалил отображение транскрипции из формы упражнений и модальных окон добавления вопросов.
- Обеспечил доступность кнопки «Проверить» после нажатия «Сдаться», что позволяет пользователю выполнить работу над ошибками после просмотра правильного ответа.
- Закомментировал блок дополнительных действий (`secondary-actions`), чтобы он не отображался в интерфейсе упражнений.
- Удалена отдельная кнопка «Тренировать» для подкатегорий в боковой панели, оставлена только общая кнопка «Тренировать все».

### 2026-07-20 (ребрендинг)

- Произвел глобальный ребрендинг проекта: заменил "Dad English" и "DadEnglish" на "My Simple English" и "MySimpleEnglish" во всем проекте.
- Обновил контактную информацию: новая почта `simple.english.simple@mail.ru`, домен `my-simple-english.ru`, телефон `+7 955 236-80-16`.
- Заменил все вхождения логотипа `/img/logo_white.svg` на `logo.png` (в хедере и футере).
- Полностью обновил юридические документы (`src/pages/page/`):
    - Сменил оператора/исполнителя на Лупенко Евгения Андреевича (ИНН 272293229271).
    - Указал новый адрес: 680038, Россия, г. Хабаровск, ул. Серышева д. 13.
    - В Политику конфиденциальности добавил администратора сайта: Чернышев Артем Алексеевич.
    - Обновил все пункты и ссылки на документы согласно новым требованиям.
- Проверил отсутствие упоминаний старого владельца (Кумицкий В.О.) и его реквизитов во всех файлах проекта.

### 2026-07-20 (перенос блога)

- Перенёс все 99 статей блога из `source/blog/*.html` в Content Collection `src/content/blog/{category}/{slug}.md` (5 категорий: A0, A1, A2, B1, B2), сохранив точную структуру и содержимое каждого урока.
- Во фронтматтере каждой статьи сохранены `title`, `date`, `excerpt`, `category`, `categoryName`, `order` (порядок совпадает с исходной постраничной лентой) и `image` (превью).
- Внутренние ссылки между статьями и пути к медиафайлам (`uploads/blog/preview`, `uploads/files/*`) автоматически исправлены на абсолютные `/blog/{category}/{slug}` и `/uploads/...` соответственно, включая межкатегорийные переходы.
- Добавил `src/lib/blog-categories.ts` (список категорий) и компонент `src/components/blog/BlogPagination.astro` (пагинация, повторяет разметку оригинала).
- Реализовал страницы: `src/pages/blog/[...page].astro` (общая лента), `src/pages/blog/[category]/[...page].astro` (лента по категории) и `src/pages/blog/[category]/[slug].astro` (отдельная статья) — все используют уже существующие `BlogLayout`, `BlogCategories`, `ArticleCard`.
- Пагинация по 20 статей на страницу воспроизводит оригинал: общая лента — 5 страниц (20+20+20+20+19), категории B1 и B2 — по 2 страницы, остальные категории — на одной странице.
- Обновил `urls-seo.txt`: добавлены все 111 адресов блога (лента, страницы категорий, страницы пагинации, статьи).
- `npm run build` проходит успешно, все внутренние ссылки и медиафайлы статей проверены на отсутствие битых путей.

### 2026-07-20

- Реализовал единую страницу `videos` вместо подхода с отдельными категориями `videos-1...videos-21`.
- Добавил слой моков `src/lib/videos-mock.ts`, который подтягивает данные категорий и карточек видео из `source/videos-*.html`.
- Добавил страницу `src/pages/videos.astro` с единым layout и переключением категорий без перехода на отдельные страницы.
- Обновил SEO-список URL: добавил `/videos` в `urls-seo.txt`.

### 2026-07-20 (продолжение)

- Вынес все категории и темы личного кабинета из `src/pages/cabinet.astro` (61к строк) в моки `src/lib/cabinet-mock.ts`.
- Реализовал React-компоненты `src/components/cabinet/CabinetSidebar.tsx` и `src/components/cabinet/CategoryModal.tsx`.
- Реализовал логику выбора тем: при клике на категорию в сайдбаре открывается модальное окно с чекбоксами под-тем.
- Добавил сохранение выбора в `localStorage` (ключи `category` и `selectedItems`).
- Оптимизировал `src/pages/cabinet.astro`: размер файла уменьшен с 61к строк до 446 строк за счёт выноса статического контента в моки.
- `npm run build` проходит успешно.

### 2026-07-20 (начало сессии)

- Исправил ошибку сборки: удалил `@astrojs/node` адаптер из `astro.config.mjs` (конфликт с `output: static`), установил недостающие зависимости (`@astrojs/sitemap`, `@astrojs/react`, `terser` и др.).
- Создал общие компоненты `src/components/shared/header/Header.astro` и `src/components/shared/footer/Footer.astro` из шаблона `source/videos.html`.
- Создал `src/layouts/PageLayout.astro` — общий layout для статических страниц с хедером, футером и скриптами.
- Перенёс `source/index.htm` → `src/pages/index.astro` с React-компонентом `SignupLoginForm` для форм входа/регистрации.
- Перенёс 6 статических страниц в `src/pages/page/`: `faq`, `kontakty-i-rekvizity`, `politika-konfidencialnosti`, `polzovatelskoe-soglasenie`, `publicnaa-oferta`, `soglasie-na-obrabotku-personalnyh-dannyh`.
- Перенёс `source/checkout.html` → `src/pages/checkout.astro` (статическая копия с тарифами).
- Перенёс `source/request-password-reset.html` → `src/pages/request-password-reset.astro` с React-компонентом `ResetPasswordForm` (отправка на n8n endpoint).
- Перенёс `source/cabinet.html` → `src/pages/cabinet.astro` как статическую HTML-оболочку (61k строк, все скрипты через `is:inline`).
- Добавил все URL в `urls-seo.txt`: `/`, `/videos`, `/cabinet`, `/checkout`, `/request-password-reset`, `/page/*`.
- `npm run build` проходит без ошибок, собрано 11 страниц.
### 2026-07-23 (Platega в личном кабинете)

- `/checkout` подключён к server-side Platega API: сумма и параметры тарифов
  выбираются только на сервере, пользователь подтверждается через текущий
  Directus access token.
- Тариф на 1 месяц оформляется как рекуррентная СБП-подписка с ежемесячным
  списанием; тарифы на 3 и 12 месяцев оплачиваются один раз.
- После создания платежа в Directus `subscriptions` сохраняются `user_id`,
  provider `transaction_id`, `expired_at: null` и `is_prologation`.
- Добавлены отдельные callback-и обычной оплаты, списания и статуса
  подписки. Доступ выдаётся только после `CONFIRMED`; активация рекуррента и
  success redirect доступ не выдают.
- Astro переведён в Node server output. Динамические страницы блога явно
  оставлены в prerender-режиме.
- Настройка окружения, Directus и callback URL описана в
  `docs/platega-directus.md`.
### 2026-07-23 (восстановление простой оплаты Platega)

- Восстановлены server-side billing-модули, удаление которых ломало сборку.
- `user_id` берётся из проверенной Directus-сессии и передаётся в Platega
  metadata; pending-запись в `subscriptions` создаётся до redirect.
- Callback-и используют `transaction_id` как correlation key и меняют только
  `expired_at`/`is_prologation` после проверки merchant, суммы, валюты и статуса.
